import type { Metadata } from "next"
import { ComingSoon } from "@/components/custom/coming-soon"

export const metadata: Metadata = { title: "Change Time In/Time Out" }

export default function ChangeTimePage() {
  return <ComingSoon title="Change Time In/Time Out" description="Review and act on employee time correction requests." />
}
